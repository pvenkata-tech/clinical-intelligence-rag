# --- Core Orchestration & AI Framework ---
# Main framework for LLM chaining
langchain==0.1.16
# Specific integration for AWS Bedrock
langchain-aws==0.1.2
# Specific integration for OpenAI
langchain-openai==0.1.3
# Specific integration for Anthropic (Claude native)
langchain-anthropic==0.1.11
# For complex state-machine AI workflows (Agents)
langgraph==0.0.32
# Core LangChain utilities
langchain-core==0.1.42
langchain-community>=0.0.32  # Required for document loading (PyPDFLoader, etc.)

# --- Infrastructure & Cloud ---
# AWS SDK for Bedrock authentication and calls
boto3==1.34.84
# Vector Database client (new package name)
pinecone>=3.0.0
# LangChain integration for Pinecone
langchain-pinecone>=0.1.0

# --- API & Web Server ---
# High-performance web framework
fastapi==0.110.1
# ASGI server to run FastAPI
uvicorn==0.29.0
# For environment variable management (.env)
python-dotenv==1.0.1
# Standard data validation
pydantic==2.7.0

# --- Document Processing ---
# Standard PDF parsing
pypdf==4.2.0

# --- Utilities & Performance ---
# For handling data arrays and vector math
numpy>=1.20,<2
# HTTP client for manual API calls if needed
httpx==0.27.0

# --- Testing ---
# Testing framework for pytest
pytest>=7.0.0